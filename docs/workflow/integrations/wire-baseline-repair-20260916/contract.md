---
status: approved
review_verdict: PASS
task_id: wire-baseline-repair
worker_model: gpt-5.6-terra
base_commit: d69da80b99ac88a246fc7502414c2215a31dde18
spec_ref: docs/workflow/tasks/wire-baseline-repair.md
---

## Task ID
wire-baseline-repair

## Role
Terra Developer, followed by fresh independent Terra QA.

## Goal
Restore Wire reproducible generation for the two verified missing dependencies.
User explicitly confirmed the isolated repair proposal on 2026-09-16. Preserve
current runtime wiring and all constructor interfaces/side effects.

## Success Criteria
1. Fixed-argument ProvideContentModerationService adapter forwards every existing
   dependency including the single ProxyRepository to NewContentModerationService.
   Register adapter instead of variadic constructor; original constructor unchanged.
2. Explicit wire.Bind connects newUserTrialConsumer to *WelfareService. Preserve
   actual settlement service wiring, initialization, lifecycle and billing behavior.
3. Focused tests demonstrate dependency forwarding and compile-time interface
   satisfaction; no tests launch background workers or use DB/provider/network.
4. go generate ./cmd/server succeeds twice; second generation has identical
   generated bytes. Inspect complete first diff: only intended provider call/name
   changes or demonstrably equivalent generator ordering are acceptable. Unrelated
   missing hooks, dependencies or behavioral drift require Planner review, no repair.
5. Focused tests, original Composite selectors, server compile, backend build,
   formatting, exact allowlist and unmerged/diff checks pass. This is local QA,
   not real provider/DB/admin runtime acceptance or P2/P3 approval.

## Allowed Paths
- backend/internal/service/wire.go
- backend/internal/service/wire_provider_adapters.go
- backend/internal/service/wire_provider_adapters_test.go
- backend/cmd/server/wire_gen.go (generator output only)
- backend/internal/handler/wire.go
- backend/internal/handler/wire_provider_adapters_test.go
- backend/internal/handler/admin/wire_provider_adapters.go
- backend/internal/handler/admin/wire_provider_adapters_test.go
- docs/workflow/worker-results/wire-baseline-repair-result.md (Developer)
- docs/workflow/qa-reports/wire-baseline-repair-qa.md (QA)

## Denied Paths
Every unlisted business path; main/other worktrees; schema/Ent generation,
go.mod/sum, migration, frontend, production/runtime config, DB/Redis/provider,
container/service startup, deploy/push/commit/reset/clean. No manual generated edits.

## Constraints
Use only E:/codex-worktrees/sub2api/wire-baseline-repair. Main's dirty Pelican
service/wire.go and wire_gen.go belong to another task. Do not alter settlement
or moderation semantics to make generation pass. New tests use TestWireProvider
prefix. Inspect constructor worker-start conditions and pass nil repos as necessary
while using safe dependency sentinels. Do not start settlement provider in tests.

## Acceptance Commands
From backend:
- go test ./internal/service -list 'WireProvider|CompositeRoute'
- go test ./internal/service -run 'WireProvider|CompositeRoute' -count=1
- go test ./internal/handler/admin -run CompositeRoute -count=1
- go test ./internal/handler -run WireProvider -count=1
- go test ./internal/handler/admin -run 'WireProvider|CafeRoom.*Reset|CompositeRoute' -count=1
- go test ./internal/server/routes -run CompositeRoute -count=1
- go generate ./cmd/server (twice, record SHA256 first/second and exact diff)
- go test ./cmd/server -run '^$' -count=1
- go build ./...
- gofmt -l internal/service/wire.go internal/service/wire_provider_adapters.go internal/service/wire_provider_adapters_test.go
At root: git diff --check; git ls-files -u; git status --short; inspect full diff.
Check and record real exit code for each. QA repeats all gates independently.

## Output
Developer report first line ### DONE/FAILED/BLOCKED: wire-baseline-repair.
QA report first line ### PASS/FAIL/BLOCKED: wire-baseline-repair.
Include changed paths, actual tests, command results, full generated diff analysis,
remaining risks. Reports use apply_patch. No claims of whole service-suite green.

## Stop Rules
No development until independent contract PASS and approved status. Additional
provider errors, unrelated generation changes, dependency mutation or denied path
requirements -> stop and return evidence to Planner. Do not reset generated output.
Terra unavailable -> BLOCKED, never silently switch model.

## Planner amendment: preserve previously handwritten generated hooks
First generation exited 0 but removed two existing hooks. Developer correctly
stopped; generated output is preserved. No change may resume before independent
amendment review PASS. The following are required to preserve existing behavior,
not new feature implementations:
- In handler/wire.go, ProvideOpenAIGatewayHandler receives *service.OpsService
  explicitly and calls the existing SetOpsService on the new handler, retaining
  coordinator assignment and all other arguments. Do not change handler logic or
  NewOpenAIGatewayHandler signature. Add regression asserting both identities.
- Add admin.ProvideCafeRoomHandler adapter taking the three current constructor
  dependencies plus service.AdminService; construct with unchanged
  NewCafeRoomHandlerWithActivation, perform the original optional type assertion
  to service.CafeQuotaResetService and call the existing setter only when supported.
  Register this adapter in handler/wire.go instead of original constructor.
  Tests must cover supported/unsupported/nil AdminService and preserve all original
  dependencies without executing reset or DB operations.
- Generated diff must preserve all baseline constructor arguments, side-effectful
  service Start/cleanup calls and these two hooks (now encapsulated in providers).
  Inspect ordering changes against constructor side effects, not just text equality.
  Any further missing hook or semantic change stops the task again.
- Re-run ALL original and added commands after amendment, then two consecutive
  successful generation runs with same SHA. Include all 8 Go allowlisted files
  in gofmt -l and complete diff review. Fresh QA repeats every gate independently.
- Preserve initial BLOCKED evidence in the report before appending amended results.

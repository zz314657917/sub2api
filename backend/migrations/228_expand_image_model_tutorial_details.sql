WITH tutorial_details (slug, details_md) AS (
    VALUES
        ('gpt-image-2', $details$
## 适用场景

- 从零生成商品主图、海报、概念图和社交媒体配图。
- 根据明确的主体、风格、构图和光线要求一次生成可直接使用的图像。
- 需要多种创意方向时，保持其他字段不变，只改写 `prompt` 后分别调用，便于比较结果。

## 参数

- `model`: 必填，固定为 `gpt-image-2`；不要把展示名称或其他模型别名写入该字段。
- `prompt`: 必填，字符串。说明要画什么、主体特征、场景、构图、镜头、光线、色彩和需要保留或避免的元素。
- `size`: 可选，字符串。使用像素尺寸或构图比例，例如 `1024x1024`；它决定画幅，不代表清晰度档位。
- `resolution`: 可选，字符串。输出清晰度档位，使用 `1k`、`2k` 或 `4k`；先用 `1k` 验证提示词，确认构图后再提高档位。
- `n`: 可选，整数。一次请求要生成的图像数量；需要多个候选时增大该值，业务代码应遍历响应中的整个 `data` 数组。

## 提示词编写

按“任务目标 + 主体 + 环境 + 构图 + 风格 + 光线/色彩 + 约束”组织提示词。例如：`为咖啡品牌生成方形新品海报，一只带有金色标签的深色咖啡杯置于暖色木桌中央，近景产品摄影，柔和侧光，背景留出上方标题空间，不出现可读文字或水印。`

将必须保留的条件写成肯定句，将不希望出现的内容明确列为约束。需要程序批量生成时，把可变内容放进模板变量，只替换商品、颜色或场景，不要同时改变所有条件。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/images/generations`，请求头包含 `Authorization: Bearer YOUR_API_KEY` 和 `Content-Type: application/json`。
- 先创建只含 `model`、`prompt`、`size`、`resolution`、`n` 的 JSON 请求；缺省字段不要用空字符串或 `null` 占位。
- 该教程对应同步最终响应。检查 HTTP 成功状态后，从 `data` 数组逐项读取 `url`，不需要对本次提交再轮询上游任务。
- 发生参数错误时，先检查模型 ID、提示词是否为空、尺寸和清晰度字段是否互换；鉴权失败时只检查本项目 API 密钥，不要把密钥写入日志。
$details$),
        ('gpt-image-2-official', $details$
## 适用场景

- 需要细调输出格式、质量、背景策略或审核策略的正式生产流程。
- 已有可公开访问的蒙版图时，按支持的编辑流程控制局部区域的生成结果。
- 需要把高保真源图交给后续设计、压缩或素材管理流程时。

## 参数

- `model`: 必填，固定为 `gpt-image-2-official`；它与普通 `gpt-image-2` 是不同的调用契约。
- `prompt`: 必填，字符串。描述最终图像和局部修改目标；使用蒙版时，还要说明蒙版覆盖区域内要生成或替换的内容。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；只控制画幅。
- `resolution`: 可选，字符串。输出清晰度档位，使用 `1k`、`2k` 或 `4k`；更高档位通常需要更长处理时间。
- `n`: 可选，整数。要返回的图像数量；消费端应处理 `data` 数组的全部元素。
- `quality`: 可选，字符串。图像质量偏好；不指定时使用服务默认。先以默认质量验证内容，再按最终素材要求提高质量。
- `background`: 可选，字符串。背景策略；常规场景使用 `auto`，需要实色背景时使用 `opaque`。本教程当前不把 `transparent` 作为可用值。
- `moderation`: 可选，字符串。内容审核策略；除非业务已确认合规要求，否则保持服务默认，避免为了绕过审核而反复重试。
- `output_format`: 可选，字符串。期望输出文件格式；由下游是否需要透明通道、无损保存或网页体积控制决定。
- `output_compression`: 可选，整数。输出压缩质量；仅在所选格式支持有损压缩时使用，数值越激进通常文件越小、细节越少。
- `mask_url`: 可选，字符串。可由服务访问的 HTTPS 蒙版图地址；仅在支持蒙版的编辑流程中传入，并使蒙版与目标画幅保持一致。

## 提示词编写

普通生成可采用“交付物 + 主体 + 画面规范”的写法。例如：`生成电商详情页首图：白色无线耳机置于浅灰背景中央，正面三分之二视角，柔和棚拍光，真实产品摄影，边缘清晰，画面不含文字。`

局部编辑时先交代不变区域，再描述蒙版区域：`保留未遮罩区域的耳机外观、角度和光线；仅将遮罩区域替换为浅蓝色几何展示台。` 不要把多个互相冲突的修改目标放在同一个请求中。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/images/generations` 和 Bearer 鉴权；先完成不带可选控制项的最小请求，再逐一添加质量、格式或蒙版字段。
- 需要蒙版时，先确认 `mask_url` 可被服务端访问、文件未过期，并只在支持编辑的请求流程中传入。
- 请求成功后从最终响应的 `data` 数组读取结果 URL；同步响应不要求提交后轮询。
- 输出格式或压缩参数报错时，先移除 `output_compression`，确认基础格式可用后再逐项恢复；不要猜测不在本教程中的背景值。
$details$),
        ('gemini-3-pro-image-preview', $details$
## 适用场景

- 需要高质量概念图、品牌视觉、复杂说明书插图或多参考图融合。
- 根据产品图、风格图和构图图保持主要视觉元素，同时用提示词定义新的场景。
- 需要把一张或多张参考图作为输入，但仍希望由模型完成整体再创作。

## 参数

- `model`: 必填，固定为 `gemini-3-pro-image-preview`。
- `prompt`: 必填，字符串。描述最终图像，不要只写“按参考图生成”；明确哪些参考元素必须保留，哪些可以变化。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；用于决定最终画幅。
- `resolution`: 可选，字符串。输出清晰度档位，使用 `1k`、`2k` 或 `4k`；高保真交付前可使用 `2k` 或 `4k`。
- `image_urls`: 可选，字符串数组。按重要性顺序传入参考图 URL；最多 14 张，避免在同一组参考图中放入相互冲突的主体或风格。
- `n`: 必填或显式传入为 `1`，整数。当前兼容流程固定为 `1`；需要多个方案时提交多次不同提示词的请求。

## 提示词编写

多参考图时给每张图分配角色：`以参考图 1 的产品轮廓为准，采用参考图 2 的陶瓷材质和参考图 3 的暖色棚拍氛围，生成横向电商横幅，产品位于右侧，左侧保留文案空间。`

优先写清主体一致性、材质、镜头和排版，再写风格词。若参考图之间存在矛盾，明确优先级，而不是让模型自行猜测。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/images/generations`，传入固定模型 ID、非空 `prompt` 和 `n: 1`。
- 无参考图时省略 `image_urls`；有参考图时传数组并在发送前校验数量不超过 14、每个 URL 可访问。
- 响应为同步最终结果，从 `data[0].url` 读取图像；不要把提交响应当成需要轮询的任务。
- 结果偏离参考图时，先减少参考图数量并明确优先级，再调整提示词；不要通过传入超过限制的参考图重试。
$details$),
        ('gemini-3-pro-image-preview-official', $details$
## 适用场景

- 对视觉细节、材质表现和参考图一致性有较高要求的产品、建筑和品牌内容。
- 需要将已有图片转化为同一视觉体系的宣传图、封面或说明图。
- 需要单张稳定输出，随后由业务端自行选择、存储和发布结果的流程。

## 参数

- `model`: 必填，固定为 `gemini-3-pro-image-preview-official`；不要用非官方页面的模型 ID 替代。
- `prompt`: 必填，字符串。说明目标主体、用途、画幅构图和从参考图继承的元素。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；它控制画面比例。
- `resolution`: 可选，字符串。输出清晰度档位，使用 `1k`、`2k` 或 `4k`；以小档位调通，再为最终交付提高档位。
- `image_urls`: 可选，字符串数组。参考图 URL 列表，最多 14 张；数组顺序应反映优先级。
- `n`: 必填或显式传入为 `1`，整数。兼容流程固定为 `1`，不能通过增大该值获得批量候选。

## 提示词编写

把业务目标写在第一句，再写参考图规则。例如：`为酒店官网生成横向封面图，保留参考图 1 的建筑轮廓和石材颜色，采用参考图 2 的傍晚暖光，镜头略低于人眼视角，前景干净，不加入人物或文字。`

当要保留品牌元素时，写出可观察的形状、颜色和位置，而不是只写“保持品牌一致”。这比笼统的风格词更容易得到可复现结果。

## AI 调用清单

- 调用 `POST https://ai.3zapi.cc/v1/images/generations`，使用 Bearer 鉴权，并显式设置 `n: 1`。
- 对参考图先做可访问性和数量校验；没有参考图时不要传空数组以外的无效占位值。
- 成功后直接读取 `data[0].url`；本页模型使用同步最终响应。
- 若画幅不符合要求，先检查 `size`；若清晰度不足，检查 `resolution`，不要把分辨率档位填进 `size`。
$details$),
        ('gemini-3-1-flash-image-preview', $details$
## 适用场景

- 需要较快生成营销配图、运营素材、资讯配图或设计草案。
- 需要利用搜索能力补充时效性主题，再把事实要求转为图像构图。
- 需要参考图和文本提示词共同决定结果，但只需每次返回一张候选图的流程。

## 参数

- `model`: 必填，固定为 `gemini-3.1-flash-image-preview`。
- `prompt`: 必填，字符串。描述图像目标；启用搜索时仍要明确最终要生成的画面，而不是只写一个搜索关键词。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`。
- `resolution`: 可选，字符串。输出清晰度档位，仅使用 `1k`、`2k` 或 `4k`；不要使用 `0.5k`。
- `image_urls`: 可选，字符串数组。参考图 URL 列表，最多 14 张；按视觉优先级排序。
- `n`: 必填或显式传入为 `1`，整数。兼容流程固定为 `1`，批量探索请提交多次请求。
- `google_search`: 可选，布尔值。是否允许使用网页搜索补充与主题相关的文字信息；仅用于确有时效性或事实依赖的场景。
- `google_image_search`: 可选，布尔值。是否允许使用图片搜索补充视觉参考；只在需要外部视觉线索时开启，并在业务侧确认素材合规。

## 提示词编写

对时效性主题，先定义视觉结果，再定义检索边界。例如：`为本周科技展生成资讯封面，使用简洁的会展大厅、机器人展示台和蓝白灯光，横向构图，左侧留标题区域；如启用搜索，只补充活动主题和城市信息，不要在图中生成可读文字。`

搜索开关不是提示词替代品。主体、构图、风格和禁止项仍必须写在 `prompt` 中；不需要外部信息时关闭两个搜索字段，以减少不确定性。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/images/generations`，设置模型 ID、`prompt`、适当的 `size`、`resolution` 和 `n: 1`。
- 只有时效性任务才加入 `google_search: true` 或 `google_image_search: true`；普通商品图、插画和已有参考图任务保持关闭或省略。
- 参考图不超过 14 张，分辨率只用 `1k`、`2k`、`4k`；请求成功后从 `data[0].url` 读取同步结果。
- 搜索或参考图导致画面偏离时，先关掉不必要的搜索字段、减少参考图，再重新组织提示词中的优先级。
$details$),
        ('gemini-3-1-flash-image-preview-official', $details$
## 适用场景

- 需要快速迭代活动配图、应用内插图或带有外部视觉线索的内容草案。
- 需要结合少量产品或品牌参考图，生成统一风格的单张图像。
- 业务侧能够明确何时可以搜索、何时必须只使用已提供素材的受控生成流程。

## 参数

- `model`: 必填，固定为 `gemini-3.1-flash-image-preview-official`。
- `prompt`: 必填，字符串。先写最终交付图，再写参考图和搜索信息的作用范围。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`。
- `resolution`: 可选，字符串。输出清晰度档位，仅使用 `1k`、`2k` 或 `4k`；`0.5k` 不是本地兼容输入。
- `image_urls`: 可选，字符串数组。最多 14 张参考图 URL；把最关键的视觉锚点放在数组前面。
- `n`: 必填或显式传入为 `1`，整数。当前兼容流程固定为 `1`。
- `google_search`: 可选，布尔值。控制是否补充网页文本信息；仅在任务内容需要最新事实时启用。
- `google_image_search`: 可选，布尔值。控制是否补充图片搜索结果；启用前由业务确认来源和使用边界。

## 提示词编写

可使用“任务 + 受控外部信息 + 视觉规范”的结构：`生成一张面向开发者大会的横向宣传图，深色舞台、简洁发光屏幕和抽象代码纹理，中心留出徽标位置；仅在需要确认大会主题时使用搜索，图中不要渲染真实品牌徽标或小字号文本。`

参考图和搜索同时使用时，明确谁决定内容、谁决定风格。比如“搜索仅用于主题，参考图 1 决定配色和材质”，能避免模型混合出不需要的元素。

## AI 调用清单

- 调用地址为 `POST https://ai.3zapi.cc/v1/images/generations`；发送前检查 `model` 精确匹配、`n` 为 `1`。
- 仅在必要时开启 `google_search` 或 `google_image_search`，并将二者视为可选的内容来源，而非必填参数。
- 对 `image_urls` 做不超过 14 张的校验；`resolution` 不得使用 `0.5k`。
- 成功响应直接返回最终图像，读取 `data[0].url`；失败时先删除最近增加的可选搜索字段，定位是哪一项造成了不确定性。
$details$),
        ('midjourney', $details$
## 适用场景

- 需要强烈艺术风格、概念设计、情绪板、插画或具有明确审美方向的视觉创作。
- 需要通过版本、风格化、随机性和特殊模式调节多种创意结果。
- 需要把一张或多张参考图与文字描述结合，探索不同画面方向。

## 参数

- `model`: 必填，固定为 `midjourney`；本页接口是 Midjourney 专用的生成路径。
- `prompt`: 必填，字符串。优先描述主体、场景、镜头、媒介和想要保留的视觉关系。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；用于控制画幅。
- `version`: 可选，字符串。选择模型版本；同一项目做对比时固定该值，避免版本差异掩盖提示词效果。
- `speed`: 可选，字符串。生成速度偏好；由业务的等待时间、成本和队列要求决定。
- `quality`: 可选，字符串或数值。渲染质量偏好；更高质量通常增加生成时间，具体可用值应与所选版本保持兼容。
- `stylize`: 可选，数值。模型自主风格化强度；更高值通常让结果更偏向模型审美，较低值更服从文字描述。
- `chaos`: 可选，数值。候选结果之间的差异强度；探索阶段提高，要求稳定复现时降低。
- `weird`: 可选，数值。非常规、实验性视觉强度；只在创意探索时使用，商品和品牌规范图通常保持低值或省略。
- `stop`: 可选，数值。提前停止的进度百分比；较早停止会保留更粗略的图像效果，应与版本能力一起验证。
- `niji`: 可选，布尔值。是否使用偏动漫审美的 Niji 模式；启用前确认所选版本支持该模式。
- `raw`: 可选，布尔值。是否减少默认风格化干预；需要模型更严格执行描述时可尝试开启。
- `tile`: 可选，布尔值。是否生成可平铺纹理；适用于背景、材质和游戏贴图，不适用于普通主体海报。
- `image_urls`: 可选，字符串数组。参考图 URL 列表；使用时在 `prompt` 中说明它们负责主体、色彩或构图的哪一部分。

## 提示词编写

使用“主体 + 环境 + 镜头 + 媒介/时代 + 光线 + 约束”的顺序。例如：`雨后的未来城市街道，低机位广角镜头，湿润路面反射霓虹，电影概念艺术，深青与洋红配色，远处有少量行人剪影，画面无文字。`

先用较低 `chaos` 和明确的构图生成可控基线；需要灵感时再逐步增加 `chaos` 或 `weird`。不要同时大幅改变版本、风格化和提示词，否则无法判断哪个因素改善了结果。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/midjourney/generations`，不要将本页请求发送到通用图像生成路径。
- 从最小 JSON 开始：`model`、`prompt`、`size`；随后一次只增加一个创意控制参数并记录结果。
- 有参考图时传 `image_urls` 数组，并在提示词说明其作用；没有参考图时省略该字段。
- 本页返回同步最终响应，从 `data` 数组读取 URL。参数报错时只保留本页列出的字段，并优先检查 `stop`、`niji`、`quality` 与 `version` 的兼容性。
$details$),
        ('doubao-seedance-4-0', $details$
## 适用场景

- 需要异步提交后获取商品图、运营配图、场景概念图或参考图驱动的生成结果。
- 需要由后端任务队列统一处理较长图像任务，并在完成后下载或转存结果 URL。
- 需要控制参考图数量与输出数量总和的批量图像生产流程。

## 参数

- `model`: 必填，固定为 `doubao-seedance-4-0`。
- `prompt`: 必填，字符串。描述要生成的内容、镜头、风格、主体约束和对参考图的继承规则。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；决定最终画幅。
- `resolution`: 可选，字符串。分辨率档位；先用较低档位完成提示词和构图验证，再为交付图提高档位。
- `n`: 可选，整数。请求生成的图像数量；`n` 与参考图数量合计不得超过 15。
- `image_urls`: 可选，字符串数组。参考图 URL 列表；与 `n` 相加不得超过 15，数组顺序按视觉优先级排列。

## 提示词编写

参考图驱动时，分别写明保留与变化的内容：`保留参考图中背包的轮廓、拉链位置和深绿色主色；生成户外岩石场景中的产品广告图，晨光侧逆光，背包位于右侧三分之一处，左侧留白，不出现文字。`

异步流程不适合无限重试。将提示词、尺寸、清晰度和参考图作为一次任务的可追溯输入保存，任务失败后根据错误信息修改一项再重新提交。

## AI 调用清单

- 使用 `POST https://ai.3zapi.cc/v1/images/generations` 提交请求，并发送 Bearer 鉴权。
- 提交前校验 `image_urls` 数量加 `n` 不超过 15；未使用参考图时省略 `image_urls`。
- 提交成功后从 `data[0].task_id` 取出任务 ID，间隔约 3 秒调用 `GET https://ai.3zapi.cc/v1/tasks/{task_id}` 查询状态。
- 仅当状态为 `completed`、`succeeded` 或 `success` 时读取 `data.result.images[0].url[0]`；状态为 `failed`、`cancelled` 或 `canceled` 时记录错误并停止轮询。
$details$),
        ('doubao-seedance-4-5', $details$
## 适用场景

- 需要更高分辨率商品图、建筑渲染、品牌主视觉或需要转存的正式交付素材。
- 需要参考图约束下的异步图像生成，并希望在最终阶段使用更高清晰度。
- 需要由应用服务保存任务 ID、恢复轮询并将完成结果同步到素材库的流程。

## 参数

- `model`: 必填，固定为 `doubao-seedance-4-5`。
- `prompt`: 必填，字符串。先说明成品用途和主体，再说明构图、材质、光线、风格和参考图规则。
- `size`: 可选，字符串。像素尺寸或构图比例，例如 `1024x1024`；控制画幅而非清晰度档位。
- `resolution`: 可选，字符串。建议仅使用 `2k` 或 `4k`；用 `2k` 做首轮确认，需要最终细节时再使用 `4k`。
- `n`: 可选，整数。生成数量；与 `image_urls` 的数量合计不得超过 15。
- `image_urls`: 可选，字符串数组。参考图 URL；与 `n` 合计不得超过 15，并优先传入不可替代的产品或品牌参考。

## 提示词编写

把高保真要求写成可观察的细节，而不是只写“高清”。例如：`生成一张竖版室内设计效果图，浅橡木地板、哑光白墙、午后从左侧窗户射入的自然光，中央是一张奶油色单人沙发，织物纹理清楚但不过度锐化，真实建筑摄影风格，无人物无文字。`

当使用 `4k` 时，先在 `2k` 确认画面，再锁定同一提示词和参考图提高分辨率。这样可以避免在高成本请求中同时调试构图和细节。

## AI 调用清单

- 向 `POST https://ai.3zapi.cc/v1/images/generations` 发送请求，模型 ID 固定为本页值，认证使用 Bearer 密钥。
- 选择 `2k` 或 `4k` 的 `resolution`，并在发送前验证参考图数量加 `n` 不超过 15。
- 从提交响应读取 `data[0].task_id`，以约 3 秒间隔请求 `GET https://ai.3zapi.cc/v1/tasks/{task_id}`；应用重启后应继续使用已保存的任务 ID，而不是重复提交。
- 成功状态下读取 `data.result.images[0].url[0]` 并尽快下载或转存；失败、取消状态立即停止轮询并保留任务响应用于排错。
$details$)
)
UPDATE tutorial_pages AS page
SET content_md = regexp_replace(
        page.content_md,
        E'## 参数\\r?\\n\\r?\\n.*?\\r?\\n## cURL',
        btrim(tutorial_details.details_md, E'\\r\\n') || E'\\n\\n## cURL',
        's'
    ),
    updated_at = NOW()
FROM tutorial_details
WHERE page.slug = tutorial_details.slug
  AND page.content_md ~ E'## 参数\\r?\\n\\r?\\n.*?\\r?\\n## cURL';

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/account"
	"github.com/Wei-Shaw/sub2api/ent/apikeyaccountbinding"
	"github.com/Wei-Shaw/sub2api/ent/caferoom"
	"github.com/Wei-Shaw/sub2api/ent/groupbuyplan"
	"github.com/Wei-Shaw/sub2api/ent/groupbuyround"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// CafeRoomQuery is the builder for querying CafeRoom entities.
type CafeRoomQuery struct {
	config
	ctx                 *QueryContext
	order               []caferoom.OrderOption
	inters              []Interceptor
	predicates          []predicate.CafeRoom
	withPlan            *GroupBuyPlanQuery
	withAccount         *AccountQuery
	withRounds          *GroupBuyRoundQuery
	withAccountBindings *APIKeyAccountBindingQuery
	withFKs             bool
	modifiers           []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CafeRoomQuery builder.
func (_q *CafeRoomQuery) Where(ps ...predicate.CafeRoom) *CafeRoomQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CafeRoomQuery) Limit(limit int) *CafeRoomQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CafeRoomQuery) Offset(offset int) *CafeRoomQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CafeRoomQuery) Unique(unique bool) *CafeRoomQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CafeRoomQuery) Order(o ...caferoom.OrderOption) *CafeRoomQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryPlan chains the current query on the "plan" edge.
func (_q *CafeRoomQuery) QueryPlan() *GroupBuyPlanQuery {
	query := (&GroupBuyPlanClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(caferoom.Table, caferoom.FieldID, selector),
			sqlgraph.To(groupbuyplan.Table, groupbuyplan.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, caferoom.PlanTable, caferoom.PlanColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAccount chains the current query on the "account" edge.
func (_q *CafeRoomQuery) QueryAccount() *AccountQuery {
	query := (&AccountClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(caferoom.Table, caferoom.FieldID, selector),
			sqlgraph.To(account.Table, account.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, caferoom.AccountTable, caferoom.AccountColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRounds chains the current query on the "rounds" edge.
func (_q *CafeRoomQuery) QueryRounds() *GroupBuyRoundQuery {
	query := (&GroupBuyRoundClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(caferoom.Table, caferoom.FieldID, selector),
			sqlgraph.To(groupbuyround.Table, groupbuyround.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, caferoom.RoundsTable, caferoom.RoundsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryAccountBindings chains the current query on the "account_bindings" edge.
func (_q *CafeRoomQuery) QueryAccountBindings() *APIKeyAccountBindingQuery {
	query := (&APIKeyAccountBindingClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(caferoom.Table, caferoom.FieldID, selector),
			sqlgraph.To(apikeyaccountbinding.Table, apikeyaccountbinding.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, caferoom.AccountBindingsTable, caferoom.AccountBindingsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CafeRoom entity from the query.
// Returns a *NotFoundError when no CafeRoom was found.
func (_q *CafeRoomQuery) First(ctx context.Context) (*CafeRoom, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{caferoom.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CafeRoomQuery) FirstX(ctx context.Context) *CafeRoom {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CafeRoom ID from the query.
// Returns a *NotFoundError when no CafeRoom ID was found.
func (_q *CafeRoomQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{caferoom.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CafeRoomQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CafeRoom entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CafeRoom entity is found.
// Returns a *NotFoundError when no CafeRoom entities are found.
func (_q *CafeRoomQuery) Only(ctx context.Context) (*CafeRoom, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{caferoom.Label}
	default:
		return nil, &NotSingularError{caferoom.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CafeRoomQuery) OnlyX(ctx context.Context) *CafeRoom {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CafeRoom ID in the query.
// Returns a *NotSingularError when more than one CafeRoom ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CafeRoomQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{caferoom.Label}
	default:
		err = &NotSingularError{caferoom.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CafeRoomQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CafeRooms.
func (_q *CafeRoomQuery) All(ctx context.Context) ([]*CafeRoom, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CafeRoom, *CafeRoomQuery]()
	return withInterceptors[[]*CafeRoom](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CafeRoomQuery) AllX(ctx context.Context) []*CafeRoom {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CafeRoom IDs.
func (_q *CafeRoomQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(caferoom.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CafeRoomQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CafeRoomQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CafeRoomQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CafeRoomQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CafeRoomQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CafeRoomQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CafeRoomQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CafeRoomQuery) Clone() *CafeRoomQuery {
	if _q == nil {
		return nil
	}
	return &CafeRoomQuery{
		config:              _q.config,
		ctx:                 _q.ctx.Clone(),
		order:               append([]caferoom.OrderOption{}, _q.order...),
		inters:              append([]Interceptor{}, _q.inters...),
		predicates:          append([]predicate.CafeRoom{}, _q.predicates...),
		withPlan:            _q.withPlan.Clone(),
		withAccount:         _q.withAccount.Clone(),
		withRounds:          _q.withRounds.Clone(),
		withAccountBindings: _q.withAccountBindings.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithPlan tells the query-builder to eager-load the nodes that are connected to
// the "plan" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CafeRoomQuery) WithPlan(opts ...func(*GroupBuyPlanQuery)) *CafeRoomQuery {
	query := (&GroupBuyPlanClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withPlan = query
	return _q
}

// WithAccount tells the query-builder to eager-load the nodes that are connected to
// the "account" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CafeRoomQuery) WithAccount(opts ...func(*AccountQuery)) *CafeRoomQuery {
	query := (&AccountClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAccount = query
	return _q
}

// WithRounds tells the query-builder to eager-load the nodes that are connected to
// the "rounds" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CafeRoomQuery) WithRounds(opts ...func(*GroupBuyRoundQuery)) *CafeRoomQuery {
	query := (&GroupBuyRoundClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRounds = query
	return _q
}

// WithAccountBindings tells the query-builder to eager-load the nodes that are connected to
// the "account_bindings" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CafeRoomQuery) WithAccountBindings(opts ...func(*APIKeyAccountBindingQuery)) *CafeRoomQuery {
	query := (&APIKeyAccountBindingClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withAccountBindings = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CafeRoom.Query().
//		GroupBy(caferoom.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CafeRoomQuery) GroupBy(field string, fields ...string) *CafeRoomGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CafeRoomGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = caferoom.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CafeRoom.Query().
//		Select(caferoom.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *CafeRoomQuery) Select(fields ...string) *CafeRoomSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CafeRoomSelect{CafeRoomQuery: _q}
	sbuild.label = caferoom.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CafeRoomSelect configured with the given aggregations.
func (_q *CafeRoomQuery) Aggregate(fns ...AggregateFunc) *CafeRoomSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CafeRoomQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !caferoom.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CafeRoomQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CafeRoom, error) {
	var (
		nodes       = []*CafeRoom{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [4]bool{
			_q.withPlan != nil,
			_q.withAccount != nil,
			_q.withRounds != nil,
			_q.withAccountBindings != nil,
		}
	)
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, caferoom.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CafeRoom).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CafeRoom{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withPlan; query != nil {
		if err := _q.loadPlan(ctx, query, nodes, nil,
			func(n *CafeRoom, e *GroupBuyPlan) { n.Edges.Plan = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAccount; query != nil {
		if err := _q.loadAccount(ctx, query, nodes, nil,
			func(n *CafeRoom, e *Account) { n.Edges.Account = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRounds; query != nil {
		if err := _q.loadRounds(ctx, query, nodes,
			func(n *CafeRoom) { n.Edges.Rounds = []*GroupBuyRound{} },
			func(n *CafeRoom, e *GroupBuyRound) { n.Edges.Rounds = append(n.Edges.Rounds, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withAccountBindings; query != nil {
		if err := _q.loadAccountBindings(ctx, query, nodes,
			func(n *CafeRoom) { n.Edges.AccountBindings = []*APIKeyAccountBinding{} },
			func(n *CafeRoom, e *APIKeyAccountBinding) {
				n.Edges.AccountBindings = append(n.Edges.AccountBindings, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CafeRoomQuery) loadPlan(ctx context.Context, query *GroupBuyPlanQuery, nodes []*CafeRoom, init func(*CafeRoom), assign func(*CafeRoom, *GroupBuyPlan)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*CafeRoom)
	for i := range nodes {
		fk := nodes[i].PlanID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(groupbuyplan.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "plan_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CafeRoomQuery) loadAccount(ctx context.Context, query *AccountQuery, nodes []*CafeRoom, init func(*CafeRoom), assign func(*CafeRoom, *Account)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*CafeRoom)
	for i := range nodes {
		if nodes[i].AccountID == nil {
			continue
		}
		fk := *nodes[i].AccountID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(account.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "account_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CafeRoomQuery) loadRounds(ctx context.Context, query *GroupBuyRoundQuery, nodes []*CafeRoom, init func(*CafeRoom), assign func(*CafeRoom, *GroupBuyRound)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*CafeRoom)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(groupbuyround.FieldCafeRoomID)
	}
	query.Where(predicate.GroupBuyRound(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(caferoom.RoundsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CafeRoomID
		if fk == nil {
			return fmt.Errorf(`foreign-key "cafe_room_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cafe_room_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CafeRoomQuery) loadAccountBindings(ctx context.Context, query *APIKeyAccountBindingQuery, nodes []*CafeRoom, init func(*CafeRoom), assign func(*CafeRoom, *APIKeyAccountBinding)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*CafeRoom)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(apikeyaccountbinding.FieldCafeRoomID)
	}
	query.Where(predicate.APIKeyAccountBinding(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(caferoom.AccountBindingsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CafeRoomID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "cafe_room_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CafeRoomQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CafeRoomQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(caferoom.Table, caferoom.Columns, sqlgraph.NewFieldSpec(caferoom.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, caferoom.FieldID)
		for i := range fields {
			if fields[i] != caferoom.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if _q.withPlan != nil {
			_spec.Node.AddColumnOnce(caferoom.FieldPlanID)
		}
		if _q.withAccount != nil {
			_spec.Node.AddColumnOnce(caferoom.FieldAccountID)
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CafeRoomQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(caferoom.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = caferoom.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *CafeRoomQuery) ForUpdate(opts ...sql.LockOption) *CafeRoomQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *CafeRoomQuery) ForShare(opts ...sql.LockOption) *CafeRoomQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// CafeRoomGroupBy is the group-by builder for CafeRoom entities.
type CafeRoomGroupBy struct {
	selector
	build *CafeRoomQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CafeRoomGroupBy) Aggregate(fns ...AggregateFunc) *CafeRoomGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CafeRoomGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CafeRoomQuery, *CafeRoomGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CafeRoomGroupBy) sqlScan(ctx context.Context, root *CafeRoomQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CafeRoomSelect is the builder for selecting fields of CafeRoom entities.
type CafeRoomSelect struct {
	*CafeRoomQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CafeRoomSelect) Aggregate(fns ...AggregateFunc) *CafeRoomSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CafeRoomSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CafeRoomQuery, *CafeRoomSelect](ctx, _s.CafeRoomQuery, _s, _s.inters, v)
}

func (_s *CafeRoomSelect) sqlScan(ctx context.Context, root *CafeRoomQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
